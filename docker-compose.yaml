services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/oklati/sahabi_backend
    container_name: backend
    restart: unless-stopped
    ports:
      - "8000:8000"